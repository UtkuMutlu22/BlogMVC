@{
    ViewData["Title"] = "Alt Kategoriler";
    ViewData["Controller"] = "Alt Kategoriler";
}
@await Component.InvokeAsync("PageHeader")
<div class="card mb-2">
    <div class="card-body">
        <table class="datatable hover  compact stripe order-column " id="mainTable"></table>

    </div>
</div>
@section scripts {
    <partial name="_DataTablesPartial" />
    <script>
        $(() => {
            var columns = [
                {
                    data: 'title',
                    title: 'Alt Kategori Adı',

                },
                {
                    data: 'category',
                    title: 'Kategori Adı',

                },
                {
                    data: 'count',
                    title: 'Blog Sayısı',

                },
            ];
            appdt('/subCategories/getdata', columns, () => { });
            createIcon();
        });
    </script>

}
